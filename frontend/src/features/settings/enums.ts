export enum SettingsTab {
  GENERAL = "General",
  KEYBINDINGS = "Keybindings",
  CALIBRATION = "Calibration",
}

export enum VideoFeedURL {
  hq = "/mjpg-hq",
  mq = "/mjpg-mq",
  lq = "/mjpg-lq",
}
